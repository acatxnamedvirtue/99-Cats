<br>
<%= link_to("Add a new cat", new_cat_url) if current_user %>

<h1>ALL CATS</h1>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <a href="<%= cat_url(cat)%>"><%= cat.name %></a>
    </li>
  <% end %>

</ul>
